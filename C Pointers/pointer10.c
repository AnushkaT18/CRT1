//Null pointer: Null pointer is a pointer which is pointing to nothing.
//in case,if we domt have address to be assigned to pointer then we can simply use NULL.
#include <stdio.h>
int main()
{
    int *ptr =NULL;
    printf ("The value of ptr is %p,ptr");
    return 0;
}

